diff --git a/visualization/fn_plot_helpers.R b/visualization/fn_plot_helpers.R
index 09b564c..3be9ee4 100644
--- a/visualization/fn_plot_helpers.R
+++ b/visualization/fn_plot_helpers.R
@@ -182,12 +182,13 @@ fn_png_name <- function(fcst_type,
                         station,
                         vlt="NA",
                         vth="NA",
+                        month="All",
                         projectname=NA_character_){
   
   # Save pngs using the following file template:
-  # PARAM-FTYPE-SCORE-XAXIS-FC-DTGSTART-DTGEND-STATION-VALID(LT)-THRESHOLD.png
+  # PARAM-FTYPE-SCORE-XAXIS-FC-DTGSTART-DTGEND-STATION-VALID(LT)-THRESHOLD-MONTH.png
   SCORE       <- paste0(fcst_type,"-",score,"-",xg_str)
-  png_fname   <- paste(param,SCORE,cycle,sdate,edate,station,vlt,vth,sep = "-")
+  png_fname   <- paste(param,SCORE,cycle,sdate,edate,station,vlt,vth,month,sep = "-")
   png_fname   <- paste0(png_fname,".png")
   if (!is.na(projectname)) {
     png_fname <- paste0(projectname,"-",png_fname)
@@ -209,6 +210,8 @@ fn_save_png <- function(p_c,
                         vth     = "NA",
                         map_ind = FALSE){
   
+  # Read month from fxoption_list (defaults to "All" if not present)
+  month <- if (!is.null(fxoption_list$month)) fxoption_list$month else "All"
 
   png_fname <- fn_png_name(fcst_type,
                            score,
@@ -220,6 +223,7 @@ fn_save_png <- function(p_c,
                            vroption_list$station,
                            vlt = vlt,
                            vth = vth,
+                           month = month,
                            projectname = fxoption_list$png_projname)
   
   #if (!is.object(p_c)){
@@ -2079,11 +2083,35 @@ get_param_classes <- function(param,par_unit,score = "freq") {
       scale_mult  <- T
       scale_round <- 0
     }
+
+  } else if (param %in% c("H,LE,SW_IN,SW_OUT,LW_IN,LW_OUT")) {
+    
+    # Assuming hPa as default
+    if (score %in% c("bias","mean_bias")) {
+      brks <- seq(-400,-50,50)
+      brks <- c(brks,-25,25,-rev(brks))
+    } else if (score == "freq") {
+      brks <- seq(-600,600,50)
+    } else {
+      brks <- seq(0,15,1)
+    }
+    
+    if (par_unit %in% c("W/m2","w/m2")) {
+      scale_fac   <- 1
+      scale_mult  <- T
+      scale_round <- 0
+    } else if (par_unit %in% c("W m-2","w m-2")) {
+      scale_fac   <- 1
+      scale_mult  <- T
+      scale_round <- 0
+    }
+
     
   } else if ((param %in% c("T2m","Td2m","Tmax","Tmin")) || 
              (grepl("T2m",param,fixed=TRUE)) || 
              (gsub('[0-9]+$','',param) == "T") ||
-             (gsub('[0-9]+$','',param) == "Td")) {
+             (gsub('[0-9]+$','',param) == "Td")||
+              grepl("^TS", param)) {
     
     # Assuming degC as default
     if (score %in% c("bias","mean_bias")) {
@@ -2095,11 +2123,11 @@ get_param_classes <- function(param,par_unit,score = "freq") {
       brks <- seq(0,10,1)
     }
     
-    if (par_unit %in% c("degc","dc","c")) {
+    if (par_unit %in% c("degc","degC","dc","c")) {
       scale_fac   <- 1
       scale_mult  <- T
       scale_round <- 0
-    } else if (par_unit %in% c("k")) {
+    } else if (par_unit %in% c("k","K")) {
       # Only need to scale for freq plots
       if (score == "freq") {
         scale_fac   <- 273.15
@@ -2112,7 +2140,9 @@ get_param_classes <- function(param,par_unit,score = "freq") {
       }
     }
     
-  } else if ((param %in% c("Q2m")) || (gsub('[0-9]+$','',param) == "Q")) {
+} else if ((param %in% c("Q2m")) ||
+           (gsub('[0-9]+$', '', param) == "Q") ||
+           grepl("^SWC", param)) {
     
     # Assuming g/kg as default
     if (score %in% c("bias","mean_bias")) {
@@ -2128,7 +2158,7 @@ get_param_classes <- function(param,par_unit,score = "freq") {
       scale_fac   <- 1
       scale_mult  <- T
       scale_round <- 0
-    } else if (par_unit %in% c("kg/kg","kg kg-1")) {
+    } else if (par_unit %in% c("kg/kg","kg kg-1","m3/m3")) {
       scale_fac   <- 1/1000
       scale_mult  <- T
       scale_round <- 4
