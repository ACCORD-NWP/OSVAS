diff --git a/visualization/fn_plot_aux_scores.R b/visualization/fn_plot_aux_scores.R
index 25f4074..7ae694f 100644
--- a/visualization/fn_plot_aux_scores.R
+++ b/visualization/fn_plot_aux_scores.R
@@ -16,6 +16,7 @@ fn_plot_aux_scores <- function(fcst_input,
                                     png_projname = NA_character_,
                                     plot_num_cases = TRUE,
                                     cmap = "Set2",
+                                    custom_colors = NULL,
                                     compare_mbr000 = TRUE,
                                     mbr_plots = FALSE,
                                     rolling_verif = FALSE,
@@ -25,7 +26,8 @@ fn_plot_aux_scores <- function(fcst_input,
                                     plot_scat = TRUE,
                                     use_parallel = FALSE,
                                     fsd = NA_character_,
-                                    fed = NA_character_){
+                                    fed = NA_character_,
+                                    month = NULL){
   
   #=================================================#
   # INITIAL CHECKS 
@@ -137,7 +139,7 @@ fn_plot_aux_scores <- function(fcst_input,
   # USER INTERACTION
   #=================================================#
   
-  lt_to_use   <- seq(3,48,3) # What lead_times to use (hours)?
+  lt_to_use   <- seq(1,48,1) # What lead_times to use (hours)?
   if (attr(fcst_input,"lt_unit") != "h") {
     stop("Lead time generalisation required - raise an issue!")
   }
@@ -221,7 +223,7 @@ fn_plot_aux_scores <- function(fcst_input,
   fig_dpi     <- 200
 
   # Define the colour scheme used in line plots 
-  mcolors     <- fn_gen_model_colors(models_vec,cmap,withobs = TRUE)
+  mcolors     <- fn_gen_model_colors(models_vec,cmap,withobs = TRUE, custom_colors = custom_colors)
   
   # Line stylces
   line_styles <- c("solid","dashed","dotted","dotdash")
@@ -290,7 +292,8 @@ fn_plot_aux_scores <- function(fcst_input,
                         "plot_num_cases"    = plot_num_cases,
                         "fig_dpi"           = fig_dpi,
                         "png_projname"      = png_projname,
-                        "score_sep"         = score_sep)
+                        "score_sep"         = score_sep,
+                        "month"             = "All")
   
   #=================================================#
   # COMPUTE VARIOUS AUX SCORES
@@ -298,12 +301,37 @@ fn_plot_aux_scores <- function(fcst_input,
   
   fcst_all <- fcst
   p_list <- list()
-  
+
+  # Derive available months from the forecast data
+  if (!("valid_month" %in% names(fcst_all))) {
+    fcst_all <- harpCore::expand_date(fcst_all, valid_dttm)
+  }
+  months_avail <- sort(unique(sprintf("%02d", fcst_all[["valid_month"]])))
+  # Always run "All" first, then individual months
+  months_to_run <- c("All", months_avail)
+  # If caller passed a specific month, restrict to that
+  if (!is.null(month)) {
+    months_to_run <- unique(c("All", as.character(month)))
+  }
+
+  # Outer loop over months (All = full period, others = per-month subsets)
+  for (mm in months_to_run) {
+
+    if (mm == "All") {
+      fcst_mm <- fcst_all
+    } else {
+      fcst_mm <- dplyr::filter(fcst_all, sprintf("%02d", valid_month) == mm)
+    }
+    if (nrow(fcst_mm) == 0) next
+
+    # Update month in fxoption_list so fn_save_png encodes it in the filename
+    fxoption_list[["month"]] <- mm
+
   # Add a loop over leadtime combinations
   for (jj in seq(1,length(lt_to_use_list))) {
     
   lt_jj     <- lt_to_use_list[[jj]]
-  fcst      <- dplyr::filter(fcst_all,lead_time %in% lt_jj)
+  fcst      <- dplyr::filter(fcst_mm,lead_time %in% lt_jj)
   leadtimes <- sort(unique(fcst[["lead_time"]]))
   if (length(leadtimes) > 1) {
     lt_used <- paste0(leadtimes[1],"_",leadtimes[length(leadtimes)])
@@ -470,9 +498,11 @@ fn_plot_aux_scores <- function(fcst_input,
   } # cycle
   
   } # Loop over leadtime lists
-  
+
+  } # Loop over months
+
   if (use_parallel) {
     fn_plot_parallel(p_list,fxoption_list)
   }
-  
+
 } # End of function
